// Vercel serverless entry point for the existing Express API.
// The full API routes remain in server.ts so local and production behavior stay aligned.
import app from "../server";

export default app;
